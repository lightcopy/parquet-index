#!/bin/bash

bin="`dirname "$0"`"
ROOT_DIR="`cd "$bin/../"; pwd`"
# Use venv python if available, otherwise fall back to default python
if [[ -f "$ROOT_DIR/venv/bin/python" ]]; then
  $ROOT_DIR/venv/bin/python $@
else
  python $@
fi
